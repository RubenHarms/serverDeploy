# serverDeploy

Ubuntu:

```
sudo apt-get update
sudo apt-get install git
cd ~/
git clone https://github.com/RubenHarms/serverDeploy.git
cd serverDeploy
sudo ./install
```

Centos

```
yum install git
cd ~/
git clone https://github.com/RubenHarms/serverDeploy.git
cd serverDeploy
./install
```
